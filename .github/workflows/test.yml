name: connect

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  test-connection:
    runs-on: ubuntu-latest
    
    steps:
    - name: Test database connectivity
      run: |
        ping runflare.com -c 5 || echo "Connection failed"
